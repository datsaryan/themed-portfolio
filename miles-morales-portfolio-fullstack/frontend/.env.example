# Base URL of the backend API, no trailing slash.
# Leave unset (or delete this file's copy, .env) to run the frontend
# standalone against the static data in src/data/resumeData.ts.
VITE_API_BASE_URL=http://localhost:8080
