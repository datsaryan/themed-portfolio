spring:
  application:
    name: stranger-things-portfolio-test

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

  mail:
    host: localhost
    port: 1025
    username: test@test.com
    password: test

app:
  jwt:
    secret: TestSecretKeyForJWTAuthenticationPortfolioHawkins1983
    expiration-ms: 3600000
  cors:
    allowed-origins: http://localhost:5173
  admin:
    username: admin
    password: hawkins1983
